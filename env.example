# ===========================================
# Stock Forecast Lab 환경 변수 설정
# ===========================================

# 프로젝트 경로 설정
PROJECT_ROOT=/Users/gimminseog/project/stock-forecast
DATA_DIR=${PROJECT_ROOT}/data
RAW_DATA_DIR=${DATA_DIR}/raw
SILVER_DATA_DIR=${DATA_DIR}/silver

# API 키 설정 (필요에 따라 추가)
# Yahoo Finance는 API 키가 필요 없음
YAHOO_FINANCE_API_KEY=

# Alpha Vantage API (옵션)
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key_here

# Quandl API (옵션)
QUANDL_API_KEY=your_quandl_api_key_here

# DART API (한국 공시정보 - OpenDART)
DART_API_KEY=your_dart_api_key_here

# 데이터베이스 설정 (옵션 - 향후 확장용)
DATABASE_URL=sqlite:///stock_data.db

# 로깅 설정
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_FILE=${PROJECT_ROOT}/logs/stocklab.log

# 백테스트 설정
DEFAULT_START_DATE=2020-01-01
DEFAULT_END_DATE=2024-12-31
DEFAULT_REBALANCE_FREQ=1M
DEFAULT_TRANSACTION_COST=0.0005
DEFAULT_SLIPPAGE=0.0002

# 대시보드 설정
DASH_HOST=127.0.0.1
DASH_PORT=8050
DASH_DEBUG=true

# 캐시 설정
CACHE_DIR=${PROJECT_ROOT}/.cache
CACHE_EXPIRE_HOURS=24

# 병렬 처리 설정
N_JOBS=4
CHUNK_SIZE=1000

# 알림 설정 (옵션)
SLACK_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=
EMAIL_SMTP_SERVER=
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=
EMAIL_PASSWORD=